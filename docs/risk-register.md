# GLOBOSVN POS — Risk Register

- **Date:** 2026-06-10 · **Status:** report only, no code changed
- Likelihood/Impact: H/M/L. "Trigger" = the real-world event that converts the latent defect into an incident.

| ID | Risk | Trigger | Likelihood | Impact | Evidence | Mitigation | Priority |
|----|------|---------|-----------|--------|----------|------------|----------|
| R-01 | **Cross-tenant sales/attendance data exposure** — any authenticated user reads all stores via owner-executed views | Any curious/malicious staff member queries `v_office_pos_sales_events` etc. via PostgREST | H (no skill needed; REST endpoint exists today) | H (multi-tenant confidentiality breach; franchise trust) | `20260604001000`, `20260609000000:248`, `20260405000003/12` — grant to `authenticated`, no `security_invoker` | `ALTER VIEW … SET (security_invoker=true)` or revoke `authenticated` (Office uses service_role) | **P0** |
| R-02 | **Unauthorized invocation of money-moving edge functions** via leaked CRON_SECRET | Anyone with repo read access (ex-collaborator, leaked clone) POSTs to function URLs | M | H (forced settlements, WeTax dispatch, daily-close manipulation) | `20260413001843…sql:28,43,58,73` — secret + URLs committed | Rotate secret → Vault; `cron.alter_job`; scrub git history | **P0** |
| R-03 | **WeTax partner-API compromise** via committed credentials/SYS_ADMIN token | Credential reuse between apitest and production | M | H (e-invoice channel = legal/tax surface) | `docs/vendor/samples/01_auth_login_plaintext.json:8-9` | Redact, rotate test account, confirm prod creds never committed | **P0** |
| R-04 | **Payment role-separation breach** — waiter/kitchen operate cashier payment UI | Staff deep-links to `/cashier` on any shared device | H | M-H (fraud/voids without cashier accountability) | `app_router.dart` — guard only on `/payments/`; matrix exists unused | Enforce `canAccessRouteForRole` on all routes + per-role routing test | **P0** |
| R-05 | **Cost/load blowup from duplicate polling** — ~2.1M PostgREST calls/day at 10 stores | Scale-out of stores/devices; Supabase plan limits | H (already happening) | M (cost, DB CPU, pool pressure) | 5 providers `Timer.periodic(2s)` ignoring `_realtimeConnected` | Copy guard from `payment_detail_screen.dart:212-227` | **P0** |
| R-06 | **Wrong daily-close figures** — sales 00:00–07:00 HCMC excluded from close | Already active for every close since `20260410000000`; worst on late-night stores | H | H (financial reporting, reconciliation vs WeTax daily totals) | `create_daily_closing` `v_closing_date::TIMESTAMPTZ` = 07:00 HCMC start | Fix window; audit/backfill historical closings; add behavioral test | **P1** |
| R-07 | **Duplicate e-invoice dispatch** — overlapping cron runs send the same job twice | WeTax latency spike makes a dispatcher run exceed 60 s | M | M-H (duplicate legal invoices; cleanup via portal) | dispatcher selects `status='pending'` with no claim/SKIP LOCKED; retry RPC TOCTOU | Claiming RPC (CAS or `FOR UPDATE SKIP LOCKED`); conditional retry UPDATE | **P1** |
| R-08 | **Tenant-wide tax-config tampering** — cashier or cross-store admin rewrites tax entities | Any cashier JWT calls `wetax-onboarding` `seller_register`/`seller_info` | M | H (e-invoice issuance depends on tax_entity) | `wetax-onboarding/index.ts:248` allowlist incl. cashier; no store scoping on writes (`:99-145`) | Restrict roles; verify target entity ∈ caller's accessible stores | **P1** |
| R-09 | **Silently stale WeTax reference data** — commons_refresh cron 401s forever | Already active (cron sends CRON_SECRET; function doesn't accept it) | H | M (payment-method/tax-rate caches drift; failures surface downstream) | cron migration `:71-79` vs `wetax-onboarding/index.ts:231-257` | Accept CRON_SECRET for that op or send service key; alert on cron HTTP failures | **P1** |
| R-10 | **Encrypted-credential migration breaks 3 of 4 WeTax functions** | Switching `partner_credentials` to encrypted password_format | M | H (e-invoice pipeline halts) | `getToken` copy-pasted ×4; only dispatcher handles `password_format`/AES | Extract `functions/_shared/wetax.ts`; single tested getToken | **P1** |
| R-11 | **Fresh-environment rebuild produces wrong schema** — dual migration series, ordering tool-dependent, duplicate table defs with drift, gaps 292–297 | New environment, disaster recovery, branch-based preview DBs | M | H (DR failure at the worst moment) | numeric `001–299` Office series interleaved with timestamp POS series; `210` vs `20260405000006` drift | Segregate/renumber Office series; tombstone 210/211; verify `schema_migrations` vs files; document canonical order | **P1** |
| R-12 | **Poison einvoice job stalls the polling batch** — one bad ref_id NPEs WT06 for all 50 jobs | Vendor-side NPE (already observed on apitest) | M | M (invoice statuses frozen up to 24 h stale sweep) | `wetax-poller/index.ts:155-185` — no per-job backoff/isolation | Batch backoff on failure; per-job retry counter → terminal | **P2** |
| R-13 | **Retry storm against WeTax** — 401/5xx retried every minute, no attempt cap | Token misconfig or vendor outage | M | M (rate-limit/blacklist risk with the e-invoice vendor) | `wetax-dispatcher/index.ts:180-204`; 409 → "dispatched" with null sid | Attempt caps → `failed_terminal`; 409 sid lookup; backoff | **P2** |
| R-14 | **Orphaned delivery sales after partial settlement failure** | Crash between settlement insert and `external_sales` link; existence check then skips re-run | L-M | H (sales permanently missing from settlement; money owed mismatch) | `generate-settlement/index.ts:99-160` — three non-atomic statements | Single RPC transaction for header+items+linkage | **P2** |
| R-15 | **Client-trusted payment totals** — server no longer verifies amount vs items | Tampered/buggy client posts wrong `p_amount` | L | H (revenue/VAT misstatement) | Step-8 `process_payment` dropped the `PAYMENT_AMOUNT_MISMATCH` check present in `20260409000000:446-463` | Re-add server-side sum verification | **P2** |
| R-16 | **Stale session state on shared terminals** — previous user's cart/payment survives logout | Every shift change (logout/login on same device) | H | M (wrong-order payment, stale table state) | `auth_provider.dart:168-172` resets auth only; ~20 non-autoDispose StateNotifiers | Invalidate session providers on logout | **P1-adjacent (treat with P1 batch)** |
| R-17 | **Undiagnosable field incidents** — no logging, 40 swallowed catches, zero daily-close tests | First real production incident | H | M (MTTR measured in days; support cannot distinguish RLS-denied from empty) | no logger in lib/; `pin_service.dart:21`; daily_closing_service untested | log wrapper + instrument catches; daily-close behavioral tests | **P1** |
| R-18 | **Payroll PIN bypass** — verify returns `true` when no PIN set; unsalted SHA-256 of 4–6 digits | Store never configured a PIN; or DB read of the hash | M | M (payroll data access) | `lib/core/services/pin_service.dart:8-30` | Fail closed; server-side salted KDF verification | **P2** |
| R-19 | **Cross-tenant photo access** — qc/attendance/po buckets only require `authenticated` | Any logged-in user lists/reads other stores' photos | M | M (staff PII imagery) | storage policies: only `payment-proofs` is store-scoped (`20260414000012`) | Store-scope the other three buckets | **P2** |
| R-20 | **Possible credentials in tracked xlsx** (`…계정정보_2026-05-25.xlsx`) | File name says "account info"; already in git history | Unknown | H if real | repo root, tracked | Audit contents now; scrub history if credentials present | **P1 (audit), then act** |
| R-21 | **Admin-screen crashes on null data** — bare `as String`/`as int` on view rows | Any null in `v_store_attendance_summary` etc. | M | L-M (admin tab unusable) | `attendance_tab.dart:372…1059` (12 sites) | Null-safe casts; later `strict-casts` lint | **P2** |
| R-22 | **Inventory drifts negative silently** | Recipe deduction exceeding stock (miscount, refunds) | M | L-M (stock data untrustworthy) | `current_stock` nullable, no CHECK; unconditional decrement | Floor + variance record, or documented negative-as-signal | **P2** |
| R-23 | **Egress/storage creep** — 10 s connectivity DB probe per device; uncompressed web QC photos; audit-log writes 2/min from idle cron | Fleet growth | M | L-M (cost) | `connectivity_service.dart:104`; `qc_service.dart:386-393`; dispatcher credential-log writes | transition-based probe; web compression; no-op run throttling | **P2** |

## Standing risk-acceptance notes (documented, not defects)

- Office app couples to POS `restaurants` physical table via service_role (CLAUDE.md §5) — accepted; do not rename physical objects.
- `stores`/`store_settings` views + legacy `get_user_restaurant_id` wrapper — expand-migrate stage, intentional.
- `rpc_compat.dart` legacy fallback shim — required until Contract stage; remove then.
- Dormant fingerprint kiosk (1,124 lines, hard-redirected route) — keep or delete is a product decision; flagged in roadmap.
