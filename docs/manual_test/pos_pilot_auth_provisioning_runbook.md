# Retired: Pilot Auth Provisioning

This production procedure is retired. Test identities and fixture restaurants
must never be created, reactivated, or used as readiness gates in the POS
production project.

Use the approved operational identity runbook instead:

```text
docs/pos/POS_PRODUCTION_AUTH_OPERATIONS_RUNBOOK.md
```

The current production allowlist is:

```text
docs/pos/pos_required_production_auth_emails.txt
```
