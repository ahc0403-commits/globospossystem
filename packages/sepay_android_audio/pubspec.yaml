name: sepay_android_audio
description: Android foreground-service audio playback for SePay announcements.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.11.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: com.globosvn.sepay_android_audio
        pluginClass: SePayAndroidAudioPlugin
