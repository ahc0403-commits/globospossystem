name: Photo Objet Vietnam Day Finalization

# Retired: Photo sales now enter POS through the Super Admin Excel import.
on:
  workflow_dispatch:

permissions:
  contents: read
  issues: write

jobs:
  finalize:
    if: ${{ false }}
    uses: ./.github/workflows/photo_objet_sales_collect_runner.yml
    with:
      executor_role: primary
      trigger_cron: ${{ github.event.schedule }}
      full_day_finalize: true
    secrets: inherit
